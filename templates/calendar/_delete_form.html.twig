<center> <form method="post" action="{{ path('calendar_delete', {'id': calendar.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calendar.id) }}">
    <a> <button class="btn btn-danger">supprimer {{ calendar.title }}</button>  </a>
</form></center>
