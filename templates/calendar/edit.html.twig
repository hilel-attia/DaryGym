{% extends "admin/bars.html.twig" %}

{% block contenpage %}
    <h1>Editer une seance</h1>

    {{ include('calendar/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('calendar_index') }}">back to list</a>

    {{ include('calendar/_delete_form.html.twig') }}
{% endblock %}
