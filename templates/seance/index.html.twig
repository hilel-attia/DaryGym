{% extends "admin/bars.html.twig" %}

{% block contenpage %}
    <h1>séances</h1>


    <ul class="nav navbar-right panel_toolbox">
        <li>
            <a href="{{ path('seance_new') }}" class="btn btn-success">NEW</a>
        </li>
    </ul>


    <table id="example" class="display table table-striped table-bordered dataminetables">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Description</th>
                <th>Date_seance</th>
                <th>Debut</th>
                <th>Fin</th>
                <th>Link</th>
                <th>supprimer</th>
                <th>modifier</th>

            </tr>
        </thead>
        <tbody>
        {% for seance in seances %}
            <tr>
                <td>{{ seance.id }}</td>
                <td>{{ seance.titre }}</td>
                <td>{{ seance.description }}</td>
                <td>{{ seance.dateSeance ? seance.dateSeance|date('Y-m-d') : '' }}</td>
                <td>{{ seance.debut ? seance.debut|date('H:i:s') : '' }}</td>
                <td>{{ seance.fin ? seance.fin|date('H:i:s') : '' }}</td>
                <td>{{ seance.link }}</td>
                <td>
                    {{ include('seance/_delete_form.html.twig') }}</td>
                <td>    <center><a> <button type="button" href="{{ path('seance_edit',{"id":seance.id  }) }}" class="btn btn-primary">modifier {{ seance.titre}}

                        </button> </a> </center></td>



            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}

        </tbody>
    </table>




{% endblock %}

