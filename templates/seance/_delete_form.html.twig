<center><form method="post" action="{{ path('seance_delete', {'id': seance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ seance.id) }}">
    <a> <button class="btn btn-danger">supprimer {{ seance.titre }}</button>  </a>
</form></center>
