{% extends "admin/bars.html.twig" %}
{% block contenpage %}
    {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
    <div><p><h2>Reponse</h2></p></div>
    {{ form_errors(form.description) }}
    {{ form_errors(form.email) }}
    {{ form_widget(form) }}
   <button type="submit" class="btn btn-success">Ajouter Reponse</button>
    {{ form_end(form) }}
{% endblock %}