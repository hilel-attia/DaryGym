{% extends 'base.html.twig' %}

{% block title %}Exercicecategorie{% endblock %}

{% block body %}
    <h1>Exercicecategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercicecategorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ exercicecategorie.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('exercicecategorie_index') }}">back to list</a>

    <a href="{{ path('exercicecategorie_edit', {'id': exercicecategorie.id}) }}">edit</a>

    {{ include('exercicecategorie/_delete_form.html.twig') }}
{% endblock %}
