{% extends 'base.html.twig' %}

{% block title %}Exercice{% endblock %}

{% block body %}

    <h1>Exercice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercice.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td> <img class="image-display" src="{{ asset('public/uploads/images' ~ exercice.image ) }}> </td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ exercice.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ exercice.description }}</td>
            </tr>
            <tr>
                <th>Video</th>
                <td>{{ exercice.video }}</td>
            </tr>
            <tr>
                <th>Docs</th>
                <td>{{ exercice.docs }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('exercice_index') }}">back to list</td>

    <a href="{{ path('exercice_edit', {'id': exercice.id}) }}">edit</a>

    {{ include('exercice/_delete_form.html.twig') }}
{% endblock %}