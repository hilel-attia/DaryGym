controllers:
    resource: '../src/Controller/'
    type: annotation
    prefix: /{_locale}
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'
excel_list:
    path:     /excel_list
    defaults: { _controller: PaginasUsersBundle:Default:ExcelExport }   